version: '3'

services:
  client:
    image: cowxing_server
    container_name: cowxing_server
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '4301:4301'
    volumes:
      - /app/node_modules
      - .:/app
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - main_network
